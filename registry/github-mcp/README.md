# GitHub MCP Registry / Marketplace Notes

No distinct public GitHub MCP Registry or Marketplace submission path was confirmed for `dnz` during this track.

Current conclusion:

- The official MCP Registry is the primary standards-based registry path.
- Smithery supports MCPB publishing for local stdio servers.
- Glama supports GitHub-repo-based listing/review.
- A separate GitHub MCP Registry or Marketplace flow is deferred until GitHub publishes a clear public submission path for MCP servers.
