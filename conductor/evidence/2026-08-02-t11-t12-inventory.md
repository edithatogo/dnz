# T11/T12 inventory — 2026-08-02

The original T00–T10 Conductor roadmap is complete. GitHub follow-up issues #28–31 and #39–41 are registered as T11/T12 in `conductor/manifest.yaml` and `conductor/state.yaml`.

Local work can validate metadata, contracts, workflows, and bounded test lanes. External DOI publication, registry acceptance, GitHub rulesets, Renovate installation, and Codecov onboarding are not local implementation outcomes and remain explicitly gated.

Local checks completed:

- `python scripts/validate-registry-metadata.py` — PASS.
- `python scripts/validate-workflow-policy.py --strict` — PASS; 14 workflows and 68 action references, all immutable.
- `python scripts/check_openapi_drift.py --report openapi-drift.json` — PASS; offline route-surface report with live revalidation limitation.
- `git diff --check` — PASS for the edited worktree.
- Read-only inspection of GitHub run `30687409986` found the monthly Zenodo publish job failed because the `digitalnz` submodule was not hydrated and `digitalnz/.zenodo.json` was absent. The workflow now checks out submodules recursively and fails fast with an explicit metadata check.

Mutation lane: the full workspace contains 646 mutants and could not complete its unmutated baseline on this Windows host because the bundled Pixi Cargo selected the incompatible MSVC linker. The script now selects the installed GNU rustup toolchain and defaults to a bounded `dnz-core/src/errors.rs` slice; the full lane remains available with `-AllMutants` for CI or a host with a working linker.

Read-only external inventory:

- GitHub `edithatogo/dnz`: repository variables `HF_REPO_ID`, `HF_ZERO_COST_REVIEWED_AT`, `GH_ZERO_COST_REVIEWED_AT`, and `RNZ_ARCHIVE_ENABLED` exist; no branch protection/ruleset was found.
- Hugging Face dataset `edithatogo/digitalnz` exists and is reachable with the configured credential.
- Zenodo authentication succeeds and existing deposits are visible.
- No external upload, DOI mint, GitHub setting change, issue update, or push was performed in this run.
- The workflow fix is local only; a hosted rerun still requires the change to be committed and pushed.
- Hosted run `30755301583` for commit `13ad4a3c` passed Rust format/lint/tests/benchmarks, MSRV, OpenAPI drift, CLI container smoke, audit/licenses/secrets/SBOM, and Python/MCP/package checks. The opt-in live DigitalNZ smoke remained skipped; cache-service annotations were non-blocking.
- Hosted run `30773596219` for commit `4608cd27` passed all hardening jobs, including the bounded mutation slice, workflow-policy validation, registry metadata validation, and optional container smoke. The live DigitalNZ smoke remained skipped by design.
- Metadata-only Hugging Face publication completed to `edithatogo/digitalnz` and verified read-only: `registry/croissant.json`, `registry/registry-submission.json`, and `registry/dataset-card.md` are present. The Dataset Viewer Croissant endpoint returned 404 for this repository shape; viewer acceptance is not claimed. DOI minting and DigitalNZ/provider acceptance remain external gates.
- Metadata-only Zenodo publication completed and verified: DOI `10.5281/zenodo.21885916`, record `https://zenodo.org/records/21885916`, containing the three registry files. DigitalNZ/provider acceptance remains the sole external T11 blocker.

No credentials, authenticated URLs, or private archive content are stored in this evidence file.
