# T10 release-hardening evidence

## Implemented slice

- Added an offline OpenAPI drift report over `conductor/contracts/digitalnz-v3.yaml` and the Rust route builders. It reports the known contract status and explicit limitations rather than silently treating network access as proof.
- Added an opt-in, low-volume, read-only DigitalNZ smoke script. It uses `Authentication-Token` when supplied and never logs the token or response body.
- Added cargo-deny policy, cargo-audit, Gitleaks, SPDX SBOM, workspace surface, package, and optional container jobs in `.github/workflows/hardening.yml`.
- Expanded CI to run workspace all-features tests and all-features Clippy.
- Added deterministic release staging/archive helpers with required legal/readme/changelog files, SHA-256 sums, and forbidden-path checks.
- Added release policy documenting MSRV, SemVer/deprecation rules, benchmark and archive checks.

## Verification

- `python scripts/check_openapi_drift.py` via the bundled Pixi Python — PASS; all three route surfaces reported present; report retains the `seed_requires_live_revalidation` contract status.
- `python scripts/verify-release-archives.py <temporary-stage>` — PASS; required files and SHA-256 manifest emitted.
- `python scripts/create-release-archive.py ...` plus ZIP inspection — PASS; archive contains `SHA256SUMS` and no `.git` paths.
- `pwsh -NoProfile -File scripts/live-digitalnz-smoke.ps1` — PASS as an intentional skip without `-RunLive`.
- Workflow YAML parsing with bundled PyYAML — PASS for CI, hardening, and release workflows.
- `cargo test --workspace --all-features` with the refreshed dependency graph — PASS; all workspace tests completed successfully after the quick-xml 0.41 compatibility fix.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` with the refreshed dependency graph — PASS.
- `cargo fmt --all -- --check` — PASS after the XML parser compatibility fix.
- Final fresh-target `cargo test --workspace --all-features` — PASS; 89 core unit tests, 12 core integration tests, 5 property tests, 7 MCP tests, and all adapter/doc-test groups passed.
- Final fresh-target `cargo clippy --workspace --all-targets --all-features -- -D warnings` — PASS.
- `pixi install --locked` and `pixi run package-metadata` — PASS after the MSRV/dependency metadata update.
- `cargo audit` — PASS with no vulnerabilities after moving optional exports from full Polars to `polars-core` plus Arrow/Parquet; the vulnerable `object_store -> quick-xml 0.39.4` path is absent. One allowed unmaintained warning remains for `paste 1.0.15`, a transitive Parquet macro dependency.
- `cargo bench -p dnz-core --bench benchmarks` — PASS; `bench_record_to_dataframe` median 120.2 µs and `bench_serde_record_parsing` median 799.8 ns after the export dependency change.

## Remaining T10 work

Hosted GitHub execution of the new license/secret/SBOM, cross-platform release, and container jobs remains required evidence. The audit job is now repo-green locally; the remaining hosted evidence is execution and artifact inspection. The opt-in live smoke is also external and may reproduce the known DigitalNZ RSS/service failure; offline contract and fixture tests remain authoritative for normal CI.
