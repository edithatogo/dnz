# T04 MCP parity slice — 2026-07-14

Implemented:

- Added MCP schemas for `get_digitalnz_record` and `get_digitalnz_more_like_this`.
- Added handlers that delegate field selection, pagination, and response normalization to `dnz-core`.
- Added mock-backed protocol tests for both tools and refreshed existing MCP fixtures for header authentication and facet pagination semantics.

Verification:

```powershell
cargo +stable-x86_64-pc-windows-gnu test -p dnz-mcp --all-features
cargo +stable-x86_64-pc-windows-gnu clippy --workspace --all-targets --all-features -- -D warnings
```

Pass: 7 MCP unit tests and workspace Clippy with warnings denied.

Remaining T04 work includes Python typed/raw result parity and public documentation updates.
