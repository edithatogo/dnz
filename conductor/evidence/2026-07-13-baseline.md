# DNZ baseline evidence

Date/time UTC: 2026-07-13

## Repository state before bootstrap merge

- Repository: `edithatogo/dnz`
- Branch: `main`
- HEAD: `b2875f2acdf9b84c1a4a3e99d66ec36ae020c14e`
- Worktree: clean (`git status --short` produced no entries)
- Remotes: `origin https://github.com/edithatogo/dnz.git` (fetch and push)
- Submodules: `digitalnz` at `dccdf402dd4b8596490200d438e08ab2c2b20f6a`; `pydnz` at `86e876a5a2a6b5f9c5be2e3240422b88ce44276d`
- Bootstrap archive: one exact root archive; ZIP entries passed traversal/absolute-path inspection.
- Manifest: parsed successfully; all 34 listed files matched declared byte counts and SHA-256 digests.

## Baseline checks

| Command | Result | Notes |
|---|---|---|
| `cargo fmt --all -- --check` | PASS | No formatting changes required. |
| `cargo metadata --no-deps --format-version 1` | PASS | Four workspace crates resolved. |
| `cargo test --workspace --all-features` | BLOCKED | Compilation stopped before project code: PATH `link.exe` is Git's POSIX linker and reports `link: extra operand`; Windows SDK/MSVC linker environment is unavailable. |

## Official contract evidence

Retrieved 2026-07-13 from the official DigitalNZ v3 search and metadata documentation and SwaggerHub OpenAPI URL listed in `conductor/contracts/digitalnz-v3.yaml`. The official search documentation confirms `per_page=0` count/facet requests, repeated `and`/`or`/`without` filters, `/v3/records.<format>`, source URL click-through guidance, and the absence of thumbnail rights from thumbnail availability. The prose/OpenAPI path and response-shape differences remain tracked as contract drift pending fixture-backed implementation.

## Bootstrap reconciliation

- Added root `AGENTS.md`, `CONDUCTOR.md`, `.codex/config.toml`, four read-only specialist agents, contracts, decisions, quality gates, roadmap, risks, state, track files, and templates from the verified seed.
- Preserved the existing `conductor/archive/`, historical tracks, templates, and product/process documents.
- Linked historical `task_plan.md`, `progress.md`, and `subagents.yaml` to the conductor source of truth.
- The bootstrap ZIP and temporary extraction directory were removed after integration and checksum verification; neither is present in the repository or staging directory.
