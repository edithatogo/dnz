# T04 CLI parity slice — 2026-07-14

Implemented:

- Added thin `record` and `more-like-this` CLI commands over the corresponding `dnz-core` builders.
- Added field selection, pagination, limits, and JSON/text/Markdown output handling.
- Added Clap integration coverage for command names, defaults, fields, pagination, and output formats.

Verification:

```powershell
cargo +stable-x86_64-pc-windows-gnu test -p dnz-cli --all-features
cargo +stable-x86_64-pc-windows-gnu clippy --workspace --all-targets --all-features -- -D warnings
```

Pass: 5 library tests, 5 binary tests, 9 CLI integration tests, 0 doctests, and workspace Clippy.

Remaining T04 work includes Python typed/raw result parity, MCP schemas for record/MLT, and public documentation updates.
